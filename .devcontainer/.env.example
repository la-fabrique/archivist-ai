# Copier ce fichier en .env et remplir les valeurs
# Le .env est ignoré par git (.gitignore)

# GitHub CLI — fine-grained token avec permissions: Contents (read/write), Pull requests (read/write)
GH_TOKEN=
